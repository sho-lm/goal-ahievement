<header class="header">
  <% if logged_in? %>
    <b><%= current_user.name %></b> ログイン中
    <%= link_to("log out", logout_path, method: :delete) %>  
  <% else %>
    <%= link_to("log in", login_path) %>
  <% end %>
</header>